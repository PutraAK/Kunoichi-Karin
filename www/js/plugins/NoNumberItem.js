// Generated by CoffeeScript 1.10.0

/*:
 * @plugindesc 特定アイテムの数量を表示しないようにする
 * @author queue
 *
 * @help
 * アイテムのメモに<数量表記なし>と記入されたアイテムは、
 * 数量が表示されなくなります。
 */

(function () {
  window.Imported = window.Imported || {};

  window.Imported.Queue_NoNumberItem = true;

  if (!window.Imported.Queue_Lib) {
    throw new Error("require QueueLib.js");
  }

  Window_ItemList.prototype.needsNumber = function (item) {
    if (item === void 0) {
      return true;
    }
    return item.note.indexOf("<数量表記なし>") === -1;
  };

  Window_ItemList.prototype.drawItemNumber = function (item, x, y, width) {
    if (this.needsNumber(item)) {
      this.drawText(":", x, y, width - this.textWidth("00"), "right");
      return this.drawText($gameParty.numItems(item), x, y, width, "right");
    }
  };
}).call(this);
